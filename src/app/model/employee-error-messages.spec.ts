import { EmployeeErrorMessages } from './employee-error-messages';

describe('EmployeeErrorMessages', () => {
  it('should create an instance', () => {
    expect(new EmployeeErrorMessages()).toBeTruthy();
  });
});
